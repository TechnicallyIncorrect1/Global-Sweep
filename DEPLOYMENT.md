# Deployment

## Local/Cloud Deployment Steps
1. Clone the repo
2. Install dependencies (Node.js: `npm install`)
3. Set `GEMINI_API_KEY` in `.env.local`
4. Run with `npm run dev`

## Docker
See Dockerfile if available or request below.

## Cloud
For cloud deployment (Vercel, etc.), follow service instructions for Node.js.
